<%= form_with(model: plane, local: true) do |form| %>
  <% if plane.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plane.errors.count, "error") %> prohibited this plane from being saved:</h2>

      <ul>
      <% plane.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name, id: :plane_name %>
  </div>

  <div class="field">
    <%= form.label :rows %>
    <%= form.number_field :rows, id: :plane_rows %>
  </div>

  <div class="field">
    <%= form.label :columns %>
    <%= form.text_area :columns, id: :plane_columns %>
  </div>

  <div class="field">
    <%= form.label :is_available %>
    <%= form.check_box :is_available, id: :plane_is_available %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
